include:
  - project: 'proxiad-academie/ops-monitoring'
    ref: experiment
    file: 'ci-templates/templates/core/.gitlab-ci-kubernetes.yml'
  - project: 'proxiad-academie/ops-monitoring'
    ref: experiment
    file: 'ci-templates/platforms/minikube-dev.yml'
  - project: 'proxiad-academie/ops-monitoring'
    ref: experiment
    file: 'ci-templates/templates/shared/variables/dev.yml'


# Just extend and override rules
deploy:angular:dev:
  extends: deploy:dev
  variables:
    APP_NAME: "angular-app"  # Only override what's different
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'